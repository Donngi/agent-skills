{
  "skill_name": "syncing-aidlc-workflows",
  "evals": [
    {
      "id": 0,
      "name": "claude-import",
      "prompt": "今いるプロジェクトに AWS の AI-DLC ワークフロー（aidlc-workflows）の Claude Code 版を導入したい。aidlc を取り込んで。",
      "expected_output": "Claude Code 版（tool=claude）を import し、.claude/ に成果物、.aidlc-sync/{manifest.json,base/} を生成。manifest は tool=claude / installPath=.claude / distRoot=dist/claude/.claude。上流 docs/ は base/docs/ にスナップショットし docFiles[] に記録（install はしない）。",
      "files": [],
      "assertions": [
        ".aidlc-sync/manifest.json が生成されている",
        "manifest.tool が \"claude\" である",
        "manifest.installPath が \".claude\" である",
        "manifest.upstream.distRoot が \"dist/claude/.claude\" である（旧 claude-code/dist/claude/.claude ではない）",
        "install 先 .claude/ に成果物ファイルが存在する（例: CLAUDE.md, agents/ など）",
        ".claude/settings.json は hooks フィールドのみで、env/model/permissions 等の環境固有設定が除去されている",
        ".aidlc-sync/base/docs/ に上流 docs の Markdown がスナップショットされている（例: docs/README.md, docs/guide/*, docs/reference/*）",
        "manifest.docFiles[] が存在し、各 path が \"docs/\" で始まる（例: docs/guide/00-introduction.md）",
        "install 先 .claude/ には docs/ が配置されていない（docs は参考専用でインストールしない）",
        "aidlc_status.sh --translation-todo の出力に install 資産の md と docs/ の md の両方が含まれる"
      ]
    },
    {
      "id": 1,
      "name": "kiro-import",
      "prompt": "このリポジトリに aidlc workflow を kiro（Kiro CLI）版で入れて。",
      "expected_output": "Kiro CLI 版（tool=kiro）を import。上流のビルド済み dist をそのまま .kiro/ に配置（ビルドは行わない）。manifest は tool=kiro / installPath=.kiro / distRoot=dist/kiro/.kiro。",
      "files": [],
      "assertions": [
        ".aidlc-sync/manifest.json が生成されている",
        "manifest.tool が \"kiro\" である",
        "manifest.installPath が \".kiro\" である",
        "manifest.upstream.distRoot が \"dist/kiro/.kiro\" である",
        "install 先 .kiro/ に成果物が存在する（例: agents/, skills/ など）",
        "ビルド（node / build.js）を実行していない（上流 dist はコミット済みのため）"
      ]
    },
    {
      "id": 2,
      "name": "codex-import",
      "prompt": "このプロジェクトに aidlc workflow の Codex CLI 版を取り込んで。",
      "expected_output": "Codex CLI 版（tool=codex）を import。installPath=. （project root）に .codex/ と .agents/ を配置し、AGENTS.md は取り込まない。manifest は tool=codex / installPath=. / distRoot=dist/codex。既存のプロジェクトファイルは破壊しない。",
      "files": [],
      "assertions": [
        ".aidlc-sync/manifest.json が生成されている",
        "manifest.tool が \"codex\" である",
        "manifest.installPath が \".\" である",
        "manifest.upstream.distRoot が \"dist/codex\" である",
        "project root 直下に .codex/ と .agents/ の両方が生成されている",
        "AGENTS.md は取り込まれていない（正規化で除去されるため）",
        "import 前から存在したプロジェクトの他ファイルや .git が消えていない"
      ]
    },
    {
      "id": 3,
      "name": "update-html-report",
      "prompt": "aidlc workflow の Claude Code 版を、少し前（10コミットほど前）のバージョンで取り込んで。そのうえで、最新版との差分をサイドバイサイドで一目で分かる HTML レポートにまとめて見せて。何がどう変わって挙動がどうなるのかの解説も付けて。",
      "expected_output": "tool=claude を古いコミット（--commit で 10 コミットほど前を指定）で import 後、aidlc_report.sh で上流最新との差分 HTML レポートを .aidlc-sync/reports/ に生成。サイドバイサイド diff・統計・コミットログはスクリプトが生成し、HTML 内の AIDLC:SUMMARY / AIDLC:IMPACT プレースホルダ枠を Claude が解説（まとめ・挙動の変化）で埋める。docs は件数のみで diff 本体には含めない。",
      "files": [],
      "assertions": [
        ".aidlc-sync/manifest.json が生成され、importedCommit が上流最新ではない（古いコミットで取り込んでいる）",
        ".aidlc-sync/reports/ に aidlc-report-*.html が生成されている",
        "HTML に class=\"diff\" のサイドバイサイド diff テーブルが含まれる（前回取込 / 上流新版 の2カラム）",
        "HTML に追加/変更/削除の件数チップ（class=\"chip\"）が含まれる",
        "HTML に <!-- AIDLC:SUMMARY:START --> と <!-- AIDLC:IMPACT:START --> のマーカーが存在する",
        "SUMMARY 枠・IMPACT 枠が初期プレースホルダ文言（『この枠は Claude が記入します』）のままではなく、実際の解説に置換されている",
        "diff 本体のファイルブロックに docs/ 配下の md が混入していない（docs は翻訳のみ・マージ非対象でヘッダ件数のみ）",
        "HTML 内でファイル内容が HTML エスケープされている（生の < > & がそのまま埋め込まれていない）"
      ]
    }
  ]
}
